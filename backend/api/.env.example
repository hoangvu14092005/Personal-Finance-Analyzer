# Env is centralized at the repository root.
#
# Use ../../.env for local secrets and ../../.env.example as the committed template.
# This file is kept only as a pointer for developers who look inside backend/api.
